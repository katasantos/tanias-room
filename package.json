{
  "dependencies": {
    "bootstrap": "^5.3.0",
    "gh-pages": "^5.0.0",
    "three": "^0.154.0"
  },
  "devDependencies": {
    "webpack": "^5.28.0"
  },
  "deploy": "gh-pages -d dist",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  }
}
