{
  "dependencies": {
    "bootstrap": "^5.3.0",
    "three": "^0.154.0"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build"
  }
}
